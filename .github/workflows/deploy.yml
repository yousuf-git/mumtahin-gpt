
# name: Deploy to AWS EC2

# on:
#   push:
#     branches:
#       - main
#   pull_request:
#     branches:
#       - main
#   workflow_dispatch:  # Allow manual trigger

# env:
#   PYTHON_VERSION: '3.11'

# jobs:
#   # Job 1: Code Quality & Testing
#   test:
#     name: Code Quality & Tests
#     runs-on: ubuntu-latest
    
#     steps:
#       - name: 📥 Checkout Code
#         uses: actions/checkout@v4
      
#       - name: 🐍 Setup Python
#         uses: actions/setup-python@v5
#         with:
#           python-version: ${{ env.PYTHON_VERSION }}
#           cache: 'pip'
      
#       - name: 📦 Install Dependencies
#         run: |
#           python -m pip install --upgrade pip
#           pip install -r requirements.txt
#           pip install flake8 pylint black
      
#       - name: 🎨 Check Code Formatting (Black)
#         run: |
#           # Check both v1_basic and v2_rag versions
#           black --check --diff v1_basic/*.py v2_rag/*.py || true
      
#       - name: 🔍 Lint with Flake8
#         run: |
#           # Stop build if there are Python syntax errors or undefined names
#           flake8 v1_basic v2_rag --count --select=E9,F63,F7,F82 --show-source --statistics
#           # Exit-zero treats all errors as warnings
#           flake8 v1_basic v2_rag --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
      
#       - name: 🔍 Lint with Pylint
#         run: |
#           # Check v2_rag (main deployment version)
#           pylint v2_rag/*.py --exit-zero
      
#       - name: ✅ Syntax Check
#         run: |
#           # Check v2_rag files
#           python -m py_compile v2_rag/app.py
#           python -m py_compile v2_rag/examiner_logic.py
#           python -m py_compile v2_rag/pdf_handler.py
      
#       - name: 📊 Generate Test Report
#         if: always()
#         run: |
#           echo "## Test Results ✅" >> $GITHUB_STEP_SUMMARY
#           echo "All code quality checks passed!" >> $GITHUB_STEP_SUMMARY

#   # Job 2: Security Scan
#   security:
#     name: Security Scan
#     runs-on: ubuntu-latest
    
#     steps:
#       - name: 📥 Checkout Code
#         uses: actions/checkout@v4
      
#       - name: 🐍 Setup Python
#         uses: actions/setup-python@v5
#         with:
#           python-version: ${{ env.PYTHON_VERSION }}
      
#       - name: 🔒 Install Safety
#         run: pip install safety
      
#       - name: 🔍 Check Dependencies for Vulnerabilities
#         run: |
#           pip install -r requirements.txt
#           safety check --json || true
      
#       - name: 📊 Security Report
#         if: always()
#         run: |
#           echo "## Security Scan 🔒" >> $GITHUB_STEP_SUMMARY
#           echo "Dependency vulnerability scan completed" >> $GITHUB_STEP_SUMMARY

#   # Job 3: Deploy to EC2
#   deploy:
#     name: Deploy to EC2
#     runs-on: ubuntu-latest
#     needs: [test, security]
#     if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    
#     steps:
#       - name: 📥 Checkout Code
#         uses: actions/checkout@v4
      
#       - name: 🔑 Setup SSH
#         run: |
#           mkdir -p ~/.ssh
#           echo "${{ secrets.EC2_SSH_KEY }}" > ~/.ssh/deploy_key
#           chmod 600 ~/.ssh/deploy_key
#           ssh-keyscan -H ${{ secrets.EC2_HOST }} >> ~/.ssh/known_hosts
      
#       - name: 🚀 Deploy to EC2
#         env:
#           EC2_HOST: ${{ secrets.EC2_HOST }}
#           EC2_USERNAME: ${{ secrets.EC2_USERNAME }}
#           GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
#         run: |
#           ssh -i ~/.ssh/deploy_key -o StrictHostKeyChecking=no ${EC2_USERNAME}@${EC2_HOST} << 'ENDSSH'
#             set -e
#             echo "🔄 Starting deployment..."
            
#             # Export GEMINI_API_KEY for deploy.sh to use
#             export GEMINI_API_KEY="${GEMINI_API_KEY}"
            
#             # Run deployment script
#             cd ~
            
#             # Run deploy.sh with -E to preserve environment variables
#             sudo -E ./deploy.sh
            
            
#             echo "✅ Deployment completed!"
#           ENDSSH
      
#       - name: 🏥 Health Check
#         run: |
#           echo "Waiting for application to start..."
#           sleep 10
          
#           # Health check with retry (update URL to your actual domain)
#           for i in {1..5}; do
#             if curl -f http://${{ secrets.EC2_HOST }}:7860 > /dev/null 2>&1; then
#               echo "✅ Health check passed!"
#               exit 0
#             fi
#             echo "Attempt $i failed, retrying..."
#             sleep 5
#           done
          
#           echo "❌ Health check failed after 5 attempts"
#           exit 1
      
#       - name: 📊 Deployment Summary
#         if: always()
#         run: |
#           echo "## Deployment Summary 🚀" >> $GITHUB_STEP_SUMMARY
#           echo "- **Branch:** ${{ github.ref_name }}" >> $GITHUB_STEP_SUMMARY
#           echo "- **Commit:** ${{ github.sha }}" >> $GITHUB_STEP_SUMMARY
#           echo "- **Actor:** ${{ github.actor }}" >> $GITHUB_STEP_SUMMARY
#           echo "- **Timestamp:** $(date)" >> $GITHUB_STEP_SUMMARY
#           echo "- **Status:** ${{ job.status }}" >> $GITHUB_STEP_SUMMARY
      
#       - name: 🔄 Rollback on Failure
#         if: failure()
#         run: |
#           echo "⚠️ Deployment failed, initiating rollback..."
#           ssh -i ~/.ssh/deploy_key -o StrictHostKeyChecking=no ${{ secrets.EC2_USERNAME }}@${{ secrets.EC2_HOST }} << 'ENDSSH'
#             cd ~
#             ./rollback.sh <<< "latest"
#           ENDSSH

#   # Job 4: Notify
#   notify:
#     name: Notification
#     runs-on: ubuntu-latest
#     needs: [deploy]
#     if: always()
    
#     steps:
#       - name: 📢 Deployment Status
#         run: |
#           if [ "${{ needs.deploy.result }}" == "success" ]; then
#             echo "✅ Deployment succeeded!"
#           else
#             echo "❌ Deployment failed!"
#           fi